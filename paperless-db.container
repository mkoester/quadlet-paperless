[Unit]
Description=Paperless-ngx database (PostgreSQL)
After=network-online.target
Wants=network-online.target

[Container]
Image=docker.io/library/postgres:18
AutoUpdate=registry

# postgres user in the postgres image runs as UID 999
# Verify: podman inspect docker.io/library/postgres:18 --format '{{.Config.User}}'
# then:   podman run --rm --entrypoint grep docker.io/library/postgres:18 postgres /etc/passwd
UserNS=keep-id:uid=999,gid=999
User=999
Group=999

Network=paperless.network

# Only POSTGRES_* vars are consumed by this container
EnvironmentFile=%h/.config/containers/systemd/paperless.env
EnvironmentFile=%h/.config/containers/systemd/paperless.override.env

# Persistent data volume. postgres:18 keeps its data tree under /var/lib/postgresql
# (the whole directory is mounted, matching the upstream Paperless-ngx compose).
Volume=%h/db:/var/lib/postgresql:Z

[Service]
Restart=always
TimeoutStartSec=900

[Install]
WantedBy=default.target
